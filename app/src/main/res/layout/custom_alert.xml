

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/dialog_bg"

    android:padding="20dp">

    <!-- FrameLayout to position the icon outside the dialog -->
    <FrameLayout

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipChildren="false"
        android:clipToPadding="false">

    <!-- Image placed at the top edge -->
    <ImageView
        android:id="@+id/icon_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/gift"
    android:layout_gravity="top|center"
        android:layout_marginTop="-20dp"
    /> <!-- Adjust to pull the image up -->

    <!-- Content inside the dialog -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
    android:gravity="center_horizontal"
    android:layout_marginTop="30dp">
    <!-- Close button at the top -->
    <ImageView
        android:id="@+id/close_button"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/closealert"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:layout_gravity="end" />

    <!-- Title -->
    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="شاهد وانسخ"
        android:fontFamily="@font/almaraibold"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:layout_marginTop="16dp" />

    <!-- Message -->
    <TextView
        android:id="@+id/message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="شاهد اعلان صغير للنسخ مجانا !واشترك في النسخة المدفوعة لحذف أي اعلانات "
        android:textColor="@android:color/white"
        android:gravity="center"
        android:layout_marginTop="8dp"
        android:textSize="14sp" />

    <!-- Watch Now Button -->
    <Button
        android:id="@+id/watch_ad_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="شاهد الان"
        android:fontFamily="@font/almaraibold"
        android:textColor="@android:color/white"
        android:backgroundTint="@color/deep_orange_400"
        android:layout_marginTop="20dp" />

    <!-- Upgrade to Pro Button -->
    <Button
        android:id="@+id/upgrade_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="الترقية الي النسخة المدفوعة"
        android:textColor="@android:color/black"
        android:fontFamily="@font/almaraibold"
        android:backgroundTint="#f2f2f2"
        android:layout_marginTop="10dp" />

</LinearLayout>
    </FrameLayout>
    </LinearLayout>